
  <div class="container container--narrow">
    <header class="page__header page__header--centered">
      <h1 class="page__title heading h1">{{ page.title }}</h1>
    </header>
  <div class="card">
    <div class="page__content rte" style="margin:25px;">
      {{ page.content }}
    </div>
  </div>
  </div>